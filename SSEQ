def sseq(s,t):
    nums=[]
    i=0
    j=0
    while i<len(s) and j<len(t):
        if s[i]==t[j]:
            nums.append(i+1)
            j+=1
        i+=1
    
    return nums
one=input()
two=input()
a=sseq(one,two)
print(" ".join(map(str,a)))
